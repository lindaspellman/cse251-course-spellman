# use threaded classes in order to return a value
# threaded function cannot return a value---b/c you need global variables to do it, and global variables have many drawbacks 

# ./ means current directory
# ../ means home directory

# don't poll too tightly---another word for pinging

# never use sleep() for synchronization